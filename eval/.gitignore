__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
build/
dist/
.venv/

# Run artifacts. Reports and transcripts are outputs, not sources -- publish a specific
# report deliberately rather than letting every local run accumulate in the history.
runs/
schemas/
label-packets.json

# Corpora are never vendored: large, separately licensed, and pinned by digest in
# datasets/fetch.py instead. See METHODOLOGY.md section 11.
corpora/
*.jsonl
