[package]
name = "marlowe-tools"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
marlowe-contract.workspace = true
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
# ADR-052 §4. Description pinning: what the user approved at install, so a live-fetched tool
# list that changed under an approved name re-asks instead of going quiet.
blake3.workspace = true
